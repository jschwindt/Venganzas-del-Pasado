- page_title('Preferencias')

.fancy
  Mi Cuenta

%h2.title= html_page_title

= devise_error_messages!

= form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { :method => :put, :class => 'form-horizontal' }) do |f|
  %legend
  .control-group
    = f.label :email, :class => 'control-label'
    .controls
      = f.text_field :email
      %span.help-block Esta dirección de mail es privada, no será publicada.
  .control-group
    = f.label :alias, :class => 'control-label'
    .controls
      = f.text_field :alias
      %span.help-block El nombre que los demás usuarios verán.
  - unless resource.has_facebook_profile?
    .control-group
      = f.label :current_password, :class => 'control-label'
      .controls
        = f.password_field :current_password
        %span.help-block Ingresar contraseña actual para guardar cambios.
    .control-group
      = f.label :password, :class => 'control-label'
      .controls
        = f.password_field :password
        %span.help-block Dejar en blanco si no se desea cambiar.
    .control-group
      = f.label :password_confirmation, :class => 'control-label'
      .controls
        = f.password_field :password_confirmation
        %span.help-block Repetir la contraseña de arriba, si deseás cambiar la actual.
          
  .form-actions
    = f.submit "Guardar Cambios", :class => 'btn primary'
    = link_to 'Cancelar', :back, :class => 'btn'

%h3.fancy Eliminar mi cuenta
%p
  Esto no puede deshacerse, por favor pensalo bien antes de hacer click, ya que
  no habrá forma de recuperar tus datos.

= link_to "Eliminar definitivamente la cuenta", registration_path(resource_name),
      :data => { :confirm => "¿Está seguro?" }, :method => :delete
