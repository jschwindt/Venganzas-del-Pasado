- page_title("Listado de Artículos")

.fancy Administración de Artículos

%h2.title= html_page_title

- if @articles.empty?
  %p No se han encontrado artículos
-else
  %table.table.table-striped
    %thead
      %tr
        %th #
        %th Título
        %th
    %tbody
      - @articles.each do |article|
        %tr
          %td= article.id
          %td= article.title
          %td.actions
            =link_to article, :target => '_blank' do
              %span.glyphicon.glyphicon-book
              Permalink
            =link_to edit_admin_article_path(article) do
              %span.glyphicon.glyphicon-edit
              Editar
            =link_to admin_article_path(article), :method => :delete, :data => { :confirm => "¿Estás seguro?" } do
              %span.glyphicon.glyphicon-fire
              Eliminar

  = paginate @articles
