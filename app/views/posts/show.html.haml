- title "Post"

%p
  %strong Title:
  = @post.title
%p
  %strong Content:
  = markdown_format @post.content
%p
  %strong Comments Count:
  = @post.comments_count
%p
  - @post.audios.each do |audio|
    = render 'player', :url => audio.url, :id => audio.id

%p
  = link_to "Edit", edit_post_path(@post)
  |
  = link_to "Destroy", @post, :confirm => 'Are you sure?', :method => :delete
  |
  = link_to "View All", posts_path

%hr/
%h3 Comentarios:
- @post.comments.order("created_at DESC").page(params[:page]).each do |comment|
  = markdown_format comment.content
  %hr
  