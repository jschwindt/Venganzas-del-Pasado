- content_for :head do
  - if params[:page].nil?
    %title Programas de La Venganza será Terrible - Venganzas del Pasado
  - else
    %title="Programas de La Venganza será Terrible - Página #{params[:page]} - Venganzas del Pasado"

%h2.fancy-title
  Todos los programas
  - unless params[:page].nil?
    %small
      pág. #{params[:page]}

.posts
  = render @posts

= paginate @posts, :window => 2
