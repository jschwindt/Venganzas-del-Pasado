.comments-form
  .row
    .span1.avatar
      %img{ :src => gravatar_url_for(current_user), :class => 'gravatar' }
    .span9.description
      = form_for [post, comment] do |f|
        %p
          = f.text_area :content, :class => 'xlarge', :rows => 5
        %p
          = f.submit 'Publicar', :class => 'btn primary'
