- page_title("Comentario ##{@comment.id} de #{link_to @post.title, @post}")

%h1.title!= link_to @post.title, @post
%h2.title!= "Comentario ##{@comment.id}"

.post-show
  =render @comment

= render 'layouts/banners/b468x60'
