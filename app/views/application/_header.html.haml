%header#header{:class => (current_page?(:root) ? 'home' : 'internal')}
  .container
    - if current_page?(:root)
      .row
        .col-md-12.title-container
          = link_to image_tag('splash.png', class: "img-responsive"), :root, title: "Vengazas del Pasado"
      .row
        .col-md-12.slogan
          %p
            Encontrá todos los archivos de audio en MP3 del programa de radio <em>La Venganza Será Terrible</em> de <strong>Alejandro Dolina</strong>
    - else
      .row
        .col-md-3.title-container
          = link_to image_tag('splash-small.png', class: "img-responsive"), :root, title: "Vengazas del Pasado"
        .col-md-9.slogan
          %p
            Encontrá todos los archivos de audio en MP3 del programa de radio <em>La Venganza Será Terrible</em> de <strong>Alejandro Dolina</strong>
